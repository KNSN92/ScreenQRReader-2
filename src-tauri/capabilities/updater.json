{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Capability for the shortcut config window",
  "windows": ["updater"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "os:default",
    "core:event:allow-listen"
  ]
}
